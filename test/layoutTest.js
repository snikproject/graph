import * as rdf from '../js/rdf.js';
import assert from 'assert';
import cytoscape from 'cytoscape';

/*
describe('layout', function()
{
  describe('#presetLayout()', function()
  {
    it('apply the positions from the json array', function()
    {
      var cy = cytoscape(
        {
          elements: [
            {data: {id: "1"}},
            {data: {id: "2"}},
            {data: {id: "3"}},
          ],
        });
      assert.equal(cy.nodes().size(),3);
    });
  });
});
*/
